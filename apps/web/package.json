{
  "name": "@clear-diff/web",
  "version": "0.1.0",
  "description": "Web UI. Reaches the node backend over WS only (type-only contract import, ADR-0008).",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun ./index.html",
    "build": "bun build ./index.html --outdir dist --minify --sourcemap=linked",
    "typecheck": "tsc -p tsconfig.json && tsc -p tsconfig.test.json"
  },
  "dependencies": {
    "@trpc/client": "^11",
    "monaco-editor": "^0.52.2"
  },
  "devDependencies": {
    "@clear-diff/node": "*",
    "@trpc/server": "^11"
  },
  "license": "MIT"
}
